# Changelog

## v0.28 · 2026-07-22 "Per My Email Below"
The IF-THEN payoff pass: a full audit of all 50 cards found the best ones textually promising consequences the game never delivered. Four of those promises are now kept, on the schedules their own copy set. Injected cards are consequences, not deals: none appear in the weekly deck, and Mondays deliver at most one.
- **A Favor From IT.** Cover for the intern who broke the table (either choice on The Handoff) and two weeks later he returns, full-time, in Access & Systems, the department that decides what every other department is allowed to touch. Fix the light above your desk the good way, bank the favor into the ledger, or ask what the badge reader actually logs and learn only that "log" has a plural. That last flag is planted, not yet consumed. On purpose.
- **The Deadline, Surfaced.** Replying "Sounds good!" to the 11:58 PM email (or forwarding it to Chris) no longer costs nothing: paragraph three, sentence two, detonates on Thursday. Scramble by six, or let it burn for Reputation and an "ownership culture" moment in Friday's preamble. Actually parsing the email now also earns a Friday beat: the deadline he forgot he set, presented as his own foresight. The email card's dominant choice was reading carefully; now the game knows it too.
- **The Fourth Cake.** "She will hand in her notice in four weeks" was always in the copy. Now it is in the calendar. Four weeks after The Team Brooks Keeps Losing, Priya's cake arrives on schedule; if you said something out loud in the room back then, she finds you first, and her personal email joins the Network (-5 in the Saturday store). If you only ledgered it, the entry closes on time, foreseen, documented, unprevented, and the ledger has its first doubt about what it is for.
- **Dave, Landed.** Three weeks after you carry the box, Dave calls from an area code that isn't the building's. Give the reference and Dave answers on the first ring forever (Network -10). The Partnership's contact who "asks for you by name" now has a reason: Dave gave them the name. The Network is officially the one store item other people pay into (Nina, Dave, Priya; floored at 5, the last stretch of a way out is always yours to fund).
- **The Monday cap.** Chain payoffs (training, balloon invoice, survey follow-up, and the three new ones) now land at most one per Monday, first due wins; the rest keep their flags and wait. They are patient. So is the building.
- **The tagline** (Decision 55): the description now leads with "the one person who actually keeps the building running" and keeps Operations Manager underneath. Role unchanged in-game; specificity is the engine.
- Verified via `node --check`, a clean dominance audit, sims v020-v027, and a new `tools/sim-v028.js`.

## v0.27 · 2026-07-21 "Development Opportunity"
The Severance/Nolan pass, planned and built in one session, plus two removals Rodolfo called for. Audio (ambient + Friday tension tone) was ruled KEPT this session (Decision 48).
- **The Presentation.** Two consecutive fully-secured weeks and the building adapts: the memo announces a development opportunity, and that Friday you present OUR WINS yourself, from slides you did not write. Read it as written and it goes well, which is the worst part: the compliance Leverage bonus halves and next week claims one extra item. Deviate and it's a roll in front of everyone. Paper Trails are never broken; the building taxes the framing, not the filing. Replaces the stance and the agenda that week; returns every second clean week. This is the answer to the lock-everything dominant strategy found in the v0.26 playtest.
- **Notes Between Selves.** Saturdays offer one free line to Monday-you: a warning (Monday's first lock costs 1 less), an encouragement (a small Monday mercy), or an instruction (if it survives review, Monday names Friday's host). It arrives stamped INTERNAL CORRESPONDENCE · REVIEWED. Usually intact. Sometimes a word is redacted. Rarely a word has been changed, and the game does not acknowledge it.
- **A Restorative Conversation.** Once per run, below 30 Reputation, Dana schedules it. You read the prepared statement aloud until SINCERITY reaches 100%; the statement gets shorter each read ("I am grateful. I recognize. I am."). Small Sanity cost per read, partial Reputation rehabilitation on release, and a new commendation: SINCERITY DETECTED.
- **The phone.** Once per save, the first time a host's composure breaks: a phone on the wall rings, they answer, listen, say "understood," and hang up. Never explained. Never repeated.
- **The Sixty-Second Sync** (new card, week 4+): a meeting that "will only take a minute," during which the real stopwatch visibly runs at double speed. It counts toward your records. The clock is not broken. The clock is attending.
- **Memory doubt.** Below 30 Sanity, one real exposed item on the evening tracker reads "you do not remember writing this." It was always there.
- **NG+ misremembers you.** Cycle 2+ copy: the week-1 boot insists there was no interruption in your service, your chair was never in another building, and the founder's portrait in the quit ending is of someone else now, and has always been.
- **REMOVED: the composure bar** (Rodolfo's call). The meter mechanic is unchanged; the host's state now reads as a line of prose ("Director Reed is rattled. The room has noticed.") that recolors as it degrades. Handbook updated.
- **REMOVED: every mention of "career fair."** The Arch is now for "the event"; there is always an event. Scrubbed from game copy, item names, changelog, and design doc.
- Verified via `node --check`, a clean dominance audit, all six prior sims (sim-v020's bar checks rewritten for the prose line), and a new `tools/sim-v027.js` (44 checks).

## v0.26.1 · 2026-07-21 (hotfix, found by playtesting live)
- **Double-clicking Continue no longer fires its action twice.** The Continue button stayed clickable behind the day-transition animation, so a double-click on an evening's result advanced two (or more) days at once; a live playthrough went Monday to Friday in one screen. It now disables itself on the first click.

## v0.26 · 2026-07-21 "The Offer Letter"
The whole 2026-07-21 designer-review list, shipped in one pass: onboarding for strangers, a shareable week summary, NEW GAME+, a build-around store item, stopwatch records, a reorganized weekly memo, and the mobile/accessibility leftovers.
- **The scripted first Monday.** A brand-new save's Monday is no longer random: Standup, then The "Quick Sync" (teaches: work becomes Credit on the tracker), then The Partnership (its best choice IS a Paper Trail). Each carries a deadpan "FROM YOUR ONBOARDING PACKET" note, and the first evening's At Risk screen explains locks and Thursday midnight in one paragraph. Fires once, ever: a fired veteran rehired at week 1 is not a new hire and is not re-onboarded.
- **The Weekly Performance Summary.** Survived and compliance weeks now end on a one-screen internal memo before Saturday: grade, host, Credit produced/retained/reassigned (with the claimed items named), Paper Trails filed, Sanity and Reputation, agenda outcome, and the week's real clock time. Auto-generated, not reviewed, retained. This is the screenshot screen.
- **NEW GAME+.** The true ending now has a door on the other side: Monday brings an offer letter from a different Everwell campus with the same org chart. Accepting starts CYCLE 2: upgrades, commendations, and leftover Leverage carry; the Escape Plan was spent getting out; the week resets to 1. Each cycle the settling surcharge runs 1 point deeper, the Escape Plan costs 15 more per item ("the market has adjusted"), and every boss gains a phase 2: wound them past half Composure and they steady themselves, one line in their own dialect, then counters land 8 points less and their Composure recovers faster for the rest of the fight. The full-wipe NEW GAME option remains. New commendation: THE REVOLVING DOOR (leave on your own terms twice, +15). THE DOOR WAS OPEN finally persists, since quitting no longer erases the file.
- **A Ledger of Your Own** (store, 25 Leverage): +1 Leverage at week's end per Paper Trail still held, permanently -1 max Time. The first build-around item: templates + ledger + THE ARCHIVIST contracts + audit weeks now stack into an actual documentation build, at the price of every day being one slot shorter.
- **Fastest week on file.** The stopwatch now keeps a personal best across the save (survived/compliant weeks only), shown on the Saturday screen and called out on the summary memo when beaten: "A PERSONAL RECORD. IT WILL NOT BE MENTIONED AGAIN."
- **The Weekly Memo.** Boot notices now lead with EFFECTIVE THIS WEEK and file everything older under STANDING POLICY · STILL IN EFFECT · STILL POLICY, instead of one undifferentiated wall that was 14 items deep by week 7. The button reads "THE WEEKLY MEMO (2 NEW · 9 STANDING)."
- **Small screens and screen readers** (Decisions Log #36 leftovers): the three fixed top-right buttons no longer march across the whole header on phones (compact under 640px, header clears them), and the card region is now an aria-live polite region. Also hardened audio startup so environments without Web Audio fail silently instead of throwing on the first boot click.
- Verified via `node --check`, a clean dominance-audit re-run, all five existing regression sims (sim-v023's store count updated for the ledger), a new `tools/sim-v026.js` (47 checks across all seven features), and three end-to-end headless playthroughs (fresh-save full week, survived-week report flow, compliance report flow).

## v0.25 · 2026-07-21 "Clean Desk Policy"
- **Removed the MON-FRI week map** (the five-node SVG under the header, shipped v0.11). The header's "MONDAY · WEEK 3" label already carries the same information, and the map's one unique signal, the diamond marks on TUE/THU choice days, is information the player re-learns within one week of play. Its CSS, markup, and render loop are gone.
- **Removed the always-visible At Risk chip from the stats bar** (shipped v0.4). It read "none" most of the time and was the widest chip in the bar. The At Risk tracker itself is untouched: the full end-of-day screen, the Thursday lock-in, and all lock/steal mechanics work exactly as before. The handbook line that pointed at the top bar now points at the end-of-day screen.
- Both cuts are UI only, no mechanics changed. Verified via `node --check`, a clean dominance-audit re-run, and all four regression sims (`sim-v020` through `sim-v023`).

## v0.24 · 2026-07-17 "Composure"
- **The fridge key.** New store item, "A Copy of the Key (Don't Ask How)," 35 Leverage, the most expensive non-escape item by a wide margin. Unlocks a 3rd Minifridge option: take a Coke yourself, +30 Sanity, -6 Reputation, every time the card comes up. A real high-risk/high-reward option, not a strict upgrade, it's the best Sanity play in the game but the only one that costs Reputation, repeatedly.
- **The Friday agenda now runs on a real Composure meter, not a fixed round count.** Design analysis first, then three sized options offered, he picked the middle one. Each boss has 70 Composure, shown as a visible bar that updates round to round. Counter wins deal real damage (35); Counter losses and Deflects both let it regenerate, so the fight has genuine back-and-forth instead of one-directional attrition. The fight now ends the moment he breaks, skipping any remaining rounds, or runs the full week's round cap if he doesn't, so length actually varies with how the fight goes: a clean 2-win streak breaks any boss from week 2 on, week 1's single round can only ever reach half by design, an all-loss week regenerates him back to full. Three outcome tiers instead of two: broken (can zero out Friday's claims entirely, plus a bigger Reputation payout on the finishing blow), shaken (the old threshold and reward, unchanged), or nothing under 50% depleted. Reuses all 12 existing boss-specific moves, no new dialogue needed beyond one shared "he broke" flourish line.
- Verified via the dominance audit and a substantially rewritten `tools/sim-v020.js`: forced win-streak breaks across all four bosses, a mixed win/loss/win sequence landing the "shaken" tier correctly, an all-loss sequence confirming full regeneration, the week-1 half-depletion case, and new DOM checks confirming the bar renders once per round and updates in place instead of duplicating.

## v0.23 · 2026-07-17 "Lunch"
Four requests in one session: new lore (the boss's ChatGPT-written emails, the locked minifridge, the balloon vendor saga), a weekly clock, harder early weeks, and a leaner store.
- **Six new cards, two of them real follow-up chains.** An Email From AVP Miller (em dashes and emoji as an in-fiction ChatGPT tell, diegetic, not a style-rule violation since the joke *is* the tell); The Minifridge (locked, boss-plus-two-directors-only, ask him directly or walk to the vending machine); The Arch (an event balloon arch, vendor unapproved), whose risky-vendor choice quietly sets up The Invoice, Still two weeks later (unapproved vendor, unpayable invoice, reuses the existing recurring-vendor-ping system instead of inventing a new one); The "Anonymous" Pulse Survey, whose honest answer sets up A Note About Your Survey Response the next Monday (HR "checking in informally" about a response that was supposedly anonymous). Plus two standalone cards, The Standing Meeting With No Agenda and The Desk Plant Directive (ties into the existing Life Outside `plant` stat). Follow-ups use the same META-flag injection pattern as `trainingNotCompleted`, no new systems.
- **Lunch is now a real daily beat, Monday-Thursday.** Eat at your desk (small Sanity cost, small Credit) or actually leave the building (costs Time, heals Sanity). Owning Standing Lunch with Chris unlocks a third, better option. Friday already had an equivalent choice baked into "The Quiet Morning," so it wasn't duplicated there.
- **A real stopwatch, not an in-fiction clock.** Starts the second you click "BEGIN WEEK" and counts actual elapsed seconds through the whole week, visibly, for both speedrunning and pressure: it doesn't stop for the interstitials, the meeting, or anything else. Turns red and pulses once you're in Crunch. Persists across weeks as a running total (survives Burnout/PMO resets too, it's a play-time stat, not run progress), shown on the Saturday store screen and, as the actual payoff, on the Two Weeks' Notice ending as "TIME CLOCKED, START TO DOOR."
- **Early-week hardening, specifically weeks 1-3** (his call, over extending the late-game ramp further): the Friday agenda goes to 2 rounds and 2 claims at week 2 instead of week 3; the Sanity settling surcharge no longer skips week 1 entirely (used to be the only fully free week); recovery gets a small taper starting week 2, ahead of the existing week 4/8 tiers; week 1 evenings carry a small 20% voluntold chance instead of a hard 0%. All announced at boot, per the standing announced-difficulty rule.
- **Store cut from 13 to 12 and reorganized into three sections** (Sanity & Time, Career & Leverage, Escape Plan). Only one item was actually safe to cut: A Real Coffee Setup, made redundant by The Templates Folder (cheaper, and it protects Credit too). The rest stayed: the 3 Escape items are the meta-goal, the 3 commendation-gated items are load-bearing for the Personnel File and FULLY VESTED, and Decoy is the design review's favorite mechanic. Existing saves that already own Coffee keep its effect (the maxTime formula still checks for it); it's just no longer purchasable new.
- Verified via `node --check` (through jsdom), a clean dominance-audit re-run, the three existing regression sims (with their week-1/week-2 hardcoded expectations updated to match the new formulas), and a new `tools/sim-v023.js` covering the new cards, the lunch beat, the clock, both follow-up chains, and the reorganized store.
- **Same-day polish pass**, after Rodolfo played the build: the stopwatch is now much bigger and bolder (it's supposed to create pressure; too easy to miss at the old size); a 13th commendation, UNDER THE WIRE (+10 Leverage, clock out survived or compliant in under 6 real minutes); the bottom-of-card "last 8 things you did" recap list is gone entirely, unnecessary clutter with no mechanical purpose; The Minifridge's "ask him directly" choice no longer partially heals away its own Sanity cost, it's a clean -4 Sanity / +1 Rep now, matching the "safer choice trades for Reputation" thesis instead of undercutting its own "humiliating" framing.

## v0.22 · 2026-07-15 "A New Badge"
Rodolfo lost for the first time (Burnout, week 3, v0.21 working as intended) and immediately found the structural hole: the game offered him week 4 anyway. Losing had no consequence beyond a smaller payout.
- **Termination protocol: losing now actually ends the run.** Burnout or Performance Managed Out resets you to week 1. The week counter, the deck unlocks, the boss rotation, the difficulty ramps: all start over, because they are all keyed to the week.
- **The file survives; the employment does not.** Store upgrades (coffee, therapy, the monitor, all of it) and every commendation persist across the firing, roguelike-style. The three Escape Plan items do not: the runway gets spent on being unemployed, the portfolio goes stale, the network cools. Each loss is narrated on the Saturday screen under EXIT PAPERWORK PROCESSED.
- **The Saturday screen after a loss ends in "CLOCK IN · WEEK 1 · A NEW BADGE"** instead of the next week number. The reset is written to disk the moment the run ends, so closing the tab mid-store still boots a fresh hire, not a resurrected week.
- Handbook gained a Termination entry so the stakes are stated up front.
- Verified via `node --check`, a clean dominance-audit re-run, and a new headless sim (`tools/sim-v022.js`): burnout at week 5 resets to week 1 and strips exactly the owned escape items while keeping upgrades and commendations; the survived path still increments normally; clock-in after termination lands on week 1.

## v0.21 · 2026-07-15 "The Personnel File"
Rodolfo beat week 7 on v0.20 with no losses, so the agreed next lever fires: week-wide pressure, plus a reason for the store to exist, plus fixes from a full code scan.
- **Emotional overhead is now billed at cost: every Sanity cost is multiplied by 1.5 (rounded up) before the settling surcharge, therapy, and Crunch apply.** A base-4 choice at week 7 now costs 11, not 9. Announced as a standing policy notice, per the announced-difficulty rule.
- **Recognition recalibrated: Reputation gains land at 70% of face value (rounded, never below 1); losses land in full.** Holding the 70 shield, winning agenda counters, and staying above the PMO floor all got real. Also announced at boot.
- **PERSONNEL FILE: a commendations board.** Twelve deadpan corporate awards tracked across runs (STILL HERE, EXCEEDS EXPECTATIONS, NOTHING WAS REASSIGNED, THE FILING SYSTEM, MINUTES REFLECT DISSENT, NOTED FIVE TIMES, THE DOOR WAS OPEN, A DEDICATED EMPLOYEE, NOT A CULTURE FIT, FULL COVERAGE, TENURE, FULLY VESTED). Each pays a one-time Leverage bonus, awarded on the Saturday screen; the full list, including unearned terms, is always visible in the store so every run has a goal.
- **Three commendation-gated store items**: A Second Monitor (+1 max Time, requires EXCEEDS EXPECTATIONS), The Old Rolodex (Monday boot names Friday's host, requires FULL COVERAGE), An Ergonomic Assessment (first Sanity cost each day lands 2 softer, requires TENURE). FULLY VESTED requires owning every non-escape item including these, closing the loop.
- **Bug fixes from a full-code scan**: Reputation hitting 0 during the Friday preamble no longer lets the meeting keep rendering over the ending screen (was an uncaught TypeError); the PTO Rep cost and Calloway's hidden Sanity tax are now guarded the same way; the Realignment log line no longer hardcodes "9/day" when upgrades change max Time.
- New Game+ selected as a direction but explicitly deferred to next session: its balance depends on this difficulty pass settling first.
- Verified via `node --check`, a clean dominance-audit re-run, and an extended headless simulation (new cost/rep math, commendation awarding on every ending type, store gating, all three item effects, mid-preamble PMO cleanup).

## v0.20 · 2026-07-15 "Items of Business"
The Friday Meeting becomes a real multi-round fight that scales every week, in direct response to "still too easy, never lost" plus "make the boss fight more dynamic."
- **The meeting now runs an agenda of items of business before the slide goes up**: one round in weeks 1-2, two from week 3, three from week 7, announced at boot as policy updates like every other escalation. There is no way to skip the agenda; Friday costs something every single week no matter how well you prepared.
- **Each round is a boss-specific move, not a generic exchange.** Twelve new encounter-style beats, three per boss, each in their own dialect: Miller reframes, retimes, and "develops" you; Reed weaponizes process, corrections, and precedent; Brooks does bits, nicknames, and encores at your expense; Calloway holds space at you until something gives. Rounds draw without replacement, so no move repeats within one meeting.
- **Every round is a real choice with scaling costs.** Deflect is safe but costs Sanity that grows with the week (and the settling surcharge and Crunch stack on top). Counter is a roll: Reputation helps, later weeks steadily hurt (up to -16 points of odds), Miller stays hardest by a flat penalty, and losses cost both Sanity and Reputation on formulas that grow with the week. Countering while losing rounds is now a genuine route to Burnout or Performance Managed Out mid-meeting.
- **Your opening stance matters for the whole fight now.** Push back at 3:59 and every counter this meeting rolls better; stay quiet and they all roll worse. The stance costs themselves are unchanged.
- **Winning most of the agenda claims one fewer item**, replacing the old single hold-your-ground gamble, and it's called out in the meeting preamble when earned. The old escalation beat and its dialogue are retired; the round system absorbs that role.
- Handbook gained an Agenda section; two new boot policy notices announce the week 3 and week 7 round escalations, keeping difficulty announced rather than hidden.
- Verified via `node --check`, a clean dominance-audit re-run, and a headless jsdom simulation across bosses and weeks (round counts, footing math, claims reduction, mid-meeting run-ending cleanup).

## v0.19 · 2026-07-12 "The Building Has Begun to Settle"
A difficulty change plus one experimental audio cue explicitly flagged for testing, not committed yet.
- **Sanity settling surcharge reworked: now +1 every week starting week 2, capped at +5 by week 6**, replacing the old flat 3-tier version (wk3 +1, wk6 +2, wk9 +3). This touches every Sanity-costing choice in the game uniformly, including the new Friday Meeting stance/escalation costs from v0.17-v0.18, so the whole back half of a run gets meaningfully harder, faster, than before. Updated the boot policy notices that narrate this (previously tied to wk3/6/9, now tied to wk2 when it starts and wk6 when it caps) so the change stays announced, not hidden, per the game's own stated difficulty philosophy.
- **EXPERIMENTAL: a faint, sustained high-pitch tone now plays through Friday afternoon**, starting at 9 AM ("The Quiet Morning") and cutting out the instant 4 PM starts (handed off to the existing meeting drone/riser). This is explicitly a test, not a final decision: Rodolfo asked to try it and see whether it's actually good or just annoying. Gated behind the ambient audio toggle so it's trivially silenceable, and built as two isolated function calls (`startFridayTension()` / `stopFridayTension()`) specifically so it's cheap to rip out if it doesn't earn its place.
- Verified via `node --check`, a clean dominance-audit re-run (unaffected, no choice deltas changed), and a headless simulation confirming the exact surcharge value at every week 1-10 and confirming the tension tone never throws, whether ambient audio is off, on-but-uninitialized, or mid-playback.

## v0.18 · 2026-07-12 "Hold Your Ground"
A follow-up on v0.17: Rodolfo asked for the boss fight to have more mechanics (more dialogue, real difficulty, its own music, something that actually breaks the week's monotony instead of feeling like just another card), plus a separate cleanup request.
- **The Friday Meeting is now a real escalating exchange, not one binary choice.** Choosing to push back at the opening no longer just ends in a flavor line, it leads into a second beat: the boss doesn't let it go either, and you get one more real decision: hold your ground (a genuine gamble) or let it drop (safe, no further cost). Staying quiet at the opening skips this entirely; there's nothing to escalate if you never pushed back.
- **Holding your ground is a real risk, with real stakes.** Win, and the meeting claims one fewer item this Friday (on top of whatever the boss rotation already determined), plus a small Reputation/Sanity recovery. Lose, and it costs meaningfully more Reputation and Sanity than staying quiet ever would have. Odds are weighted by your current Reputation (a stat you've been building all week, now has a use at the moment that matters most), get worse at higher weeks, and are deliberately worse against Miller specifically than any of the three directors, keeping him hardest on yet another axis.
- **New dialogue**: 3 opening rebuttal lines, 3 win lines, 3 loss lines, all boss-name-aware so the exchange still reads as being with whichever of the four is running that day.
- **The whole meeting now sounds and looks different from the rest of the week.** A new `inBossFight` flag pushes the existing Sanity/week-driven dread music noticeably further for the entire meeting (not just a one-off sting), and a new pulsing red vignette (`body.confrontation`) wraps the screen for the same span, both switched on the moment 4 PM starts and guaranteed to clear afterward no matter which ending is reached (survived, malicious compliance, quit, Burnout, or Performance Managed Out).
- **Removed the ID badge boot graphic** (the small circular silhouette added in v0.12/v0.13), along with its CSS, HTML, and the `.glitchy` escalation logic tied to it. The badge-photo-doubling motif stays in the narrative text (Burnout ending, Two Weeks' Notice ending) since only the visual asset itself was cut, not the idea.
- Verified via `node --check`, a clean dominance-audit re-run, and a headless jsdom simulation forcing both the win and lose paths through the full sequence: confirmed correct claims reduction on a win, correct Reputation/Sanity deltas on both outcomes, and confirmed the confrontation visual/audio state is present throughout the meeting and fully cleared after every ending path.

## v0.17 · 2026-07-12 "Before He Starts Talking"
A scope correction: what Rodolfo actually asked for last session was a boss *battle*, an interactive confrontation with real stakes, not just a rotating cast running the same passive reveal-and-claim sequence. That's a real gap, not a misunderstanding on his end.
- **The Friday Meeting is no longer purely passive.** Right as the room fills in, before any names get read, you now get one real choice: meet the boss's eyes and push back out loud (costs Reputation, zero Sanity hit), or say nothing and let it happen (costs Sanity, no Reputation change). This is the first point in the entire meeting where the player does something instead of watching it happen to them.
- **Sized deliberately small.** Presented three options (a per-claim defense choice, this confrontation beat, and a full turn-based composure-meter combat system) and Rodolfo picked the middle one: reuses the exact card/choice UI every other encounter in the game already uses, touches only Sanity/Reputation (not claims, targeting, or shield threshold, which stayed untouched to protect last session's tuning), and ships as one clean binary choice rather than a new sub-system. The composure-meter option was flagged as likely scope creep for a first pass.
- **Side benefit: this directly helps the standing "hard to lose under good play" problem** (flagged since v0.3-era notes, still open as of v0.13-v0.16). The Friday Meeting previously cost almost no Sanity on its own; now every single week guarantees a real Sanity choice regardless of how well you prepared, adding a genuine weekly pressure valve toward Burnout that pure preparation can't fully route around.
- Verified via `node --check`, the dominance audit (clean, not flagged since delta profiles diverge on opposite axes with no strict winner), and a headless jsdom run driving both choices through to the existing reveal/verdict, confirming correct Sanity/Reputation application and a clean handoff into the rest of the meeting.

## v0.16 · 2026-07-12 "Cultural Fit"
A playtest report on the boss rotation, one real bugfix, and a new director card.
- **Bug report investigated: "boss mechanic didn't trigger by week 3."** Verified the rotation logic itself is correct (re-read `pickFridayBoss()`/`buildWeek()`, no resets, matches last session's 4,000-trial simulation). The actual explanation: weeks 1-2 are hardcoded to Miller by design, so week 3 is the first week a substitute can appear at all, and there's still a 55% chance Miller gets picked again on that first roll, so seeing Miller at week 3 is the single most likely outcome, not a bug. Asked Rodolfo whether to keep it fully random or make substitutes more predictable (pity timer / strict rotation); he chose to keep it random as originally designed.
- **Bugfix: a week-3 boot notice still said "DIRECTOR MILLER."** Leftover from before the AVP rename in v0.15. Fixed to "AVP MILLER," with the line reworded to acknowledge that the claims-count escalation applies structurally to whoever's covering, not just Miller by name.
- **New card: "The Team Brooks Keeps Losing"** (unlocks week 5). Fictionalizes a real dynamic Rodolfo asked to add: a director whose "jokes" are dismissive and demeaning toward women on his team, not predatory, just never corrected, with a visible pattern of women quitting as a result, attached to Director Brooks (previously the least-defined of the three, a natural fit since Reed and Calloway already had distinct personalities). Framed through the game's existing institutional-non-accountability lens rather than as a personal conflict: HR would file it under "cultural fit," same as it always does. Mechanically mirrors the existing Farewell Cake / Cake Count pattern (speak up in the moment for an immediate personal cost and a Reputation hit, or document it quietly for a delayed, impersonal Leverage payoff) rather than inventing a new system. Audited clean against the dominance checker (the documentation choice uses `after`, same pattern the checker already treats as intentionally non-comparable, per Decision 29).
- Verified via `node --check` and a clean dominance-audit re-run.

## v0.15 · 2026-07-12 "Whoever Shows Up"
Requested feature: the Friday Meeting is no longer always Miller.

- **Miller is now AVP Miller**, sitting above a roster of three directors (Reed, Brooks, and a new third: Calloway). The Handbook's Friday-mechanic explanation was rewritten to describe a rotation instead of one fixed person; Per Director Reed's flavor text was adjusted so its "other two directors" line still tracks now that there are exactly two others besides Miller.
- **Any of the four can run 4 PM, revealed only when the slide goes up.** Weeks 1-2 are always Miller, so the player learns the baseline before the roster starts rotating; week 3 onward it's a weighted roll (Miller 55%, Reed/Brooks/Calloway 15% each), chosen once at the start of each week (`buildWeek()`) and kept secret until `meetingProper()` runs. When a substitute is covering, a short absence line explains where Miller is (aligning with Upstairs, letting Reed cover; "volunteering" Brooks; Calloway covering "with love").
- **Miller is mechanically the hardest on every axis, not just flavor.** Substitutes claim one fewer item than Miller would at every difficulty tier (guaranteed by formula, not tuned by hand), and Miller's Reputation shield threshold (70) is the highest of the four, meaning he's the hardest to protect your best item against even though he's otherwise the "default" boss. Reed (65), Brooks (55), and Calloway (45) are each easier to shield against, in that order.
- **Each director plays differently, not just reads differently:**
  - **Director Reed** targets whichever item is already NOTICED, not the highest-value one, and is immune to the Decoy Deliverable trinket entirely, she doesn't work off the same tell everyone else does.
  - **Director Brooks** uses the same value-based targeting and decoy interaction as Miller, just claims less.
  - **Director Calloway** has a hidden Sanity tax: mid-meeting she asks the room to "share a rose and a thorn," costing Sanity that isn't shown anywhere on the At Risk screen going in, on theme for a toxic-positivity character whose real cost only shows up after you've already agreed.
- **New card: "A Note From Director Calloway"** (unlocks week 2), introducing her ahead of her first possible Friday appearance: a DM asking you to post a "win" in `#gratitude`, with genuinely different trade-offs (performing gratitude publicly for Reputation vs. a private honest reply that costs Reputation but saves Life Outside), audited clean against the dominated-choice checker.
- **Credit-flow text branches by who's running the room.** Miller's version is unchanged ("presents it Upstairs to The President"); a substitute's version notes the credit converts to their Raise Points "on paper," but still structurally rolls up to Miller on the org chart, keeping the satire's actual target (the structure, not whichever individual is standing at the podium) intact. The bigger end-of-run summary line (`scoreBlock()`, "Claimed by AVP Miller") and the Two Weeks' Notice / Malicious Compliance endings were deliberately left anchored to Miller specifically, on the reasoning that those are epilogue beats about beating the institution as a whole, not about whichever director happened to run that particular Friday.
- Verified via `node --check`, a re-run of the dominance audit (clean, including the new Calloway card), and a headless jsdom simulation forcing each of the four bosses with a fixed item set: confirmed correct claims counts per boss/week tier, Reed's noticed-first targeting and decoy immunity, Brooks' decoy compatibility, Calloway's hidden Sanity tax applying through the real cost pipeline (including the existing settling-surcharge scaling), asymmetric shield thresholds (Calloway shielded at Rep 50, Miller was not), and a ~55/15/15/15 boss distribution over 4,000 simulated week-5 rolls with weeks 1-2 always Miller across 20 trials each.

## v0.14 · 2026-07-11 "No Free Lunch"
A deck-wide audit, triggered by one more sharp catch: the Old Friend fix from v0.12 added a real trade-off (`life`) to the choice, but the button label never displayed it, so the choice still looked exactly as dominated as before, because the only thing the player can see is the label.
- **Bugfix: `costLabel()` didn't render the `life` effect**, so any choice trading Sanity against Life Outside looked like a free upgrade on the button, even when the underlying numbers were fine. Fixed by passing `ch.life` into `costLabel()` and rendering it as `+N Life Outside` / `-N Life Outside` alongside Time/Sanity/Credit/Rep.
- **Built an automated dominance audit** (every card in `CARDS`, all of `EVENINGS`, `APPRECIATION`) that flags any choice pair where one option is equal-or-better than the other on every visible axis. It surfaced 7 more dominated pairs beyond Old Friend, all pre-existing, not introduced this session: **The Clarification** (holding the silence beat nodding along on every axis), **The Escalation Path** (deciding unilaterally beat waiting for zero reason to ever wait), **The Elevator** (nodding back beat speaking, with speaking giving literally nothing), **The All-Staff Survey** (lying beat honesty outright), **An Exit Interview, Not Yours** (the kind/vague answer beat honesty on every axis, including time), **Terry's Office** (email beat the in-person search on speed and Sanity both), **The Annual Appreciation Event** (full engagement beat minimal attendance on both heal and Rep), the 1:1's **Deflect** option (dominated by two of its three siblings at once), and **The Partnership**'s witnessed-close option (slower, costlier, *and* less credit than the reckless option, with no compensating benefit).
- **Rebalanced all 7**, reusing existing axes instead of inventing new ones: several now trade Reputation against the safer/quieter option (compliance is rewarded, assertion carries risk, consistent with the game's own thesis); An Exit Interview and The Annual Appreciation Event now trade against Life Outside, same pattern as Old Friend; The Partnership's careful close now grants `lockItem: true` (the item arrives pre-secured, immune to the Friday claim, at a real credit discount), mirroring the existing Dashboard-for-Upstairs card instead of introducing a new mechanic.
- Verified with the same automated audit re-run (zero dominated pairs remain across the full deck) plus a full headless jsdom playthrough (zero runtime errors).

## v0.13 · 2026-07-11 "The Cost of Leaving"
Two more live-playtest reports: a visual bug and a difficulty signal (beaten 3+ times, zero losses).
- **Bugfix: badge-photo silhouette bled outside its circle.** The shoulders path in the new boot visual extended past the outer ring at the bottom corners. Added an SVG `clipPath` matching the ring so the silhouette is always cropped to the circle regardless of exact path coordinates, instead of relying on hand-tuned numbers staying inside the boundary.
- **Bugfix: THE STOIC (and any contract) always read "not met" on the Two Weeks' Notice ending**, even when the condition was clearly satisfied (reported: ended with 97 Sanity, contract required 60+, still showed not met). Root cause: the quit ending returns immediately and never calls `awardLeverage()`, which is the only place `S.contractMet` was ever set. Fixed by evaluating `S.contract.check()` directly in the quit branch. Since that ending wipes the save immediately (new game, not continue), the display now shows "MET" without an inaccurate "+4 Leverage" promise that would never actually be banked.
- **Escape Plan costs raised 25/25/30 → 40/40/45 (80 → 125 total).** Signal: escaped 3+ times, never lost once. The Leverage economy let a strong week (Grade A, contract met) reach escape in ~5-6 weeks, meaning most players never saw the week-8/9 difficulty tiers (the harshest ones) before winning. At the new total, a near-perfect week (~18 Leverage) needs ~7 weeks minimum; a solid-but-not-perfect week (~8 Leverage) needs ~16. This was the "slow the escape" option, chosen over sharpening the early game or hand-tuning loss conditions, specifically because it's a single legible number change (visible directly in the store) rather than new hidden systems, and it structurally forces exposure to the ramp that already exists instead of building a new one. Flagged for Rodolfo to re-test: if this makes the mid-game feel like a slog rather than tense, the next lever is the Leverage income formula itself, not another cost bump.

## v0.12 · 2026-07-11 "The Decoy"
Two bugfixes from live playtest feedback, plus Phase 3 of the design-review implementation plan.
- **Bugfix: evening events could repeat two nights running.** `eveningEvent()` picked from the EVENINGS pool with plain `Math.random()` and no memory of what ran last, so the same beat (reported: "Old Friend, Late Call" twice in a row) could land back to back. Added `S.lastEvening` tracking and a `pickEvening()` helper that excludes last night's pick whenever another option exists. Verified with 20,000 simulated draws: zero consecutive repeats.
- **Bugfix: Old Friend's choice had no real trade-off.** Both options healed Sanity with no downside, and the "worse" option (+5) was strictly dominated by the better one (+12), so there was never a reason to pick it. Rebalanced against the existing Life Outside system instead of a flat number: going out heals less (+8) but feeds the friend meter (`life: 1`); the rain check heals more (+12, now the numerically "optimal" choice) but costs it (`life: -1`). The efficient play is now quietly the isolating one, on theme for a game about grind culture, and the friend-meter cost gives the "worse" option a real reason to exist.
- **A Decoy Deliverable (new store trinket, 18 Leverage).** The first item in the store that changes *how* a system plays out instead of nudging a number, per the design review's Phase 3 ask. Thursday night, if owned, name one unlocked item as the one Director Miller finds first; he claims it ahead of the natural value-sort at Friday's meeting, doesn't change how many items he takes, changes which one. Lets a cheap item absorb a claim that would otherwise have landed on your best one. Verified in a headless run: a decoy set on a 4-value item protected a 14-value item ("The Dashboard") that would otherwise have been the natural top target.
- **Unreliable narrator, below 30 Sanity.** Building on the existing narrator-inversion text drift, the terminal now occasionally (1 card in 5, while dissociating) misstates what a choice costs: the button's label is untouched, but the actual Sanity hit on click runs 2-5 higher than advertised, revealed only in the result text ("That cost more than it said it would."). Never applied to choices that skip the result screen, so the discrepancy always has somewhere to land. Verified via simulation at the expected trigger rate.
- **Badge-photo boot visual.** A small circular inline-SVG silhouette (no likeness, hand-coded) now appears at boot, captioned "EMPLOYEE ID · PHOTO ON FILE," tying into the existing "your badge photo has been retaken, you were not present for this" boot line. From week 5 on, or once 2+ Escape Plan items are owned, it quietly doubles on itself in a slow flicker, a literal "photo on file" that doesn't quite match what's actually there anymore.

## v0.11 · 2026-07-11 "The Building Draws Itself"
Phase 2 of the design-review implementation plan: presentation upgrades that build on Phase 1, no new mechanics.
- **The five-day week is now a visible map**, not just a text label: five inline-SVG nodes (Mon-Fri) under the header, connected by a line, filling in as the week is played (done/current states) so the "where am I in the week" question is answered at a glance instead of just read in the day label. Tuesday and Thursday carry a small diamond mark above their node, flagging them as choice days before you even get there. All hand-coded SVG, no image assets.
- **Inline SVG icons for all six stats** (Sanity pulse-line, Time clock face, Credit price-tag, Reputation shield, PTO palm-leaf, At Risk target), replacing text-only labels in the stat bar. Same zero-dependency approach as the audio system: generated, not imported.
- **Favicon and social preview image.** A tiny inline-SVG favicon (data URI, matches the Sanity pulse-line icon) plus a `.ico`/apple-touch-icon fallback, and a hand-generated `og-image.png` (1200x630, same terminal aesthetic, DejaVu Sans Mono, CC0/permissive system font) so the GitHub Pages listing and any shared link get a real preview instead of a blank card.
- All three verified with a headless jsdom run across a full Mon-Fri week: day-map nodes advance correctly (done/current) with zero runtime errors.

## v0.10 · 2026-07-11 "Your Call"
Phase 1 of the 2026-07-11 design review's implementation plan (see `Friday_Meeting_Design_Review_2026-07-11.md`): the roguelike-agency fix plus two presentation upgrades.
- **Tuesday and Thursday are now a real choice, not a fixed sequence.** Those two days' three free slots are dealt face-up (title + tag only, no flavor or choices spoiled) as a "YOUR CALL" screen; pick one to resolve it now, and the pool shrinks to two, then the last card plays automatically once it's the only one left. Monday and Wednesday keep their fixed Standup/1:1-first structure. This is the fix flagged in the design review as the single biggest gap versus genre siblings (Slay the Spire, Balatro): the player never chose what happened next. Now they do, twice a week. Verified end-to-end with a headless jsdom run driving real clicks through the chooser: pool shrinks correctly (3 → 2 → 1) with no duplicate or dropped cards, no re-rolled outcomes (oneOnOne's coin flip and elevator's one-time flag are safe because the card objects are built once, up front, and reused, never re-invoked to "preview" them).
- **Real terminal font:** swapped system Courier for [Share Tech Mono](https://fonts.google.com/specimen/Share%2BTech%2BMono) (Google Fonts, free, OFL-licensed), loaded via a single `<link>`, with Courier New as fallback if the font fails to load. Chosen over VT323 for legibility at the small sizes (0.7–0.9em) most of the UI runs at.
- **Sanity now visibly sickens the screen, continuously**, not just at the one-shot Burnout glitch: `#app`'s CSS filter now scales saturation, contrast, and a slow hue-rotate with `(100 - Sanity)/100`, recalculated every render and eased with a 1.2s transition. At full Sanity the screen looks exactly as before; near zero it's desaturated, higher-contrast, and faintly off-hue.

## v0.9 · 2026-07-11 "Year Two"
- **New ending, Performance Managed Out:** Reputation hitting 0 now ends the run, same as Sanity hitting 0 (Burnout), with its own title, flavor, Leverage payout (flat 2), and itemized "WHERE YOUR STANDING WENT" recap. Time and Credit deliberately were not given hard-zero loss states: Time hits 0 every day by design (that's Crunch), and Credit starts at 0 every Monday and routinely returns to 0 right after the Friday Meeting, so a zero-loss on either would trigger constantly rather than mean anything.
- **Difficulty extended into "year two":** heal amounts now shrink over time (wk4+ -15%, wk8+ -30%, "wellness resources optimized"), the settling surcharge gets a third step (wk9+ +3), lock cost gets a second step (wk9+ +2 total, "documentation now requires a third signature"), base PTO drops to 1/week from wk6+ ("PTO requests now require Director approval"), and the quota-week checkpoint climbs with the ramp (15 → 18 at wk4+ → 20 at wk7+). All of it announced at boot, nothing hidden.
- **10 new cards**, including two built directly from Rodolfo's own workplace lore: *Mandatory Refresher* (complete IT's training this week, get told next Monday it "shows incomplete" and must be redone, a real payoff for a delayed joke) and *The Cross-Functional Project* (named lead of a cross-department project, everyone bails the day of the presentation). Plus Doug's Third Tracker, Your Work Anniversary, The Badge Reader, Coffee with Nina Again, Director Reed's Audit, An Exit Interview Not Yours, and Terry's Office. Deck is 53 cards, 49 in the normal draw pool.
- **Boot screen decluttered:** the growing wall of POLICY UPDATE/NOTICE lines now collapses into a single "▸ POLICY NOTICES (N) · CLICK TO REVIEW" line, opened on demand in a modal.
- **Daily Standup's chairs change, subtly, every week:** 12 rotating unexplained details (an extra chair, tighter spacing, a chair facing the door, a warm seat nobody sat in, and more).

## v0.8 · 2026-07-11 "The Building Remembers"
- **Company renamed:** Corestone Dynamics is now **Everwell Group**, tying the company's own name into the wellness satire already in the game (the Mandatory Wellness Module, the Wellness Check-In evening event).
- **Small choices now echo on Friday instead of just resolving in the moment.** Two or more declined evening voluntolds raise the Reputation bar for the Friday shield from 70 to 80 ("Busy weekend?" delivered live at 4 PM, -4 Rep); two or more accepted with zero declines lowers it to 65 and adds a "Reliable" beat instead. The Rumor now pays off Friday too (squash it: +2 Rep at the Meeting; leave it: -3 Rep). An HR inquiry now gets your top item Noticed and earns a bitter aside from Miller on Friday. An unauthorized Facilities fix now arrives Noticed. Hanging vs. slow-walking Walsh's portrait sets a same-day mood flag that shaves or adds 1 Time to that day's lock cost.
- **The boss's claim line now varies:** six variants for "he takes credit for your work" at the Friday Meeting, picked with no immediate repeat, so the theft beat doesn't read as copy-pasted week over week.
- **The File Cabinet:** new top-bar button and modal with two sections, Completed Contracts (every week's optional objective, met or not) and Saved Files (the "+Leverage, the ledger grows" beats from The Offboarding, The Line Item, The Matcha Machine, Farewell Cake, The Austerity Memo, and the new Backfill card, now a real visible list). Also added a free "file one item away" pick in the Saturday store, once a week.
- **Eight new cards** (The Kudos Channel, Password Reset Again, The Post-Meeting Meeting, The Printer Ate Something Important, Onboarding Buddy: Jordan, The All-Staff Survey, The Shared Calendar, The Backfill That Never Comes), five unlocked at week 1. Week 1's pool was only 12 cards against 10 draws (83% of the deck shown every week); it's now 17 (about 59%). Deck is 40 cards by week 7.
- **The boot screen is quieter:** the growing wall of "POLICY UPDATE" / "NOTICE" lines is now collapsed into a single "▸ POLICY NOTICES (N) · CLICK TO REVIEW" line, opened on demand in a modal, instead of typed out in full every Monday.
- **The chairs at Daily Standup now change, subtly, every week:** one more chair than last week, tighter spacing, a chair facing the door, one short, a warm seat nobody sat in, a name in pencil that isn't anyone's, exactly as many chairs as employees, a circle that's quietly not a circle anymore, and more. Twelve variants, never explained.

## v0.7 · 2026-07-11 "Fridays Are Different Here"
- **Difficulty up a notch:** evening recovery reduced across the board (the trains run shorter now); the settling surcharge starts week 3 (+1 Sanity on everything) and deepens week 6 (+2); from week 7 Miller claims THREE items ("HE IS BEING CONSIDERED FOR SOMETHING"); and from week 3 the tracker notices your best exposed item by itself every Wednesday, no human involved.
- **Every week arrives differently:** 8 rotating boot greetings ("YOUR CHAIR HAS BEEN ADJUSTED. NOT BY YOU.") and 8 rotating building-status lines ("CONFERENCE ROOM B REMAINS OUT OF SERVICE. THE TABLE IS BEING STUDIED."). The goal is also on the boot screen now: once you own Escape items, the terminal notes your outside activity ("THIS IS NOT A THREAT. IT IS A COUNT."), and at 3/3: "THE LETTER IN YOUR BAG HAS BEEN NOTED. IT HAS ALWAYS BEEN NOTED."
- **Boot theme:** first click on the title screen wakes a slow music-box figure over the drone, each note trailed by a detuned ghost of itself. The melody ends when you clock in; the drone, like the job, continues.
- **Fridays are special:** the FRIDAY title card turns red and arrives on a rising "flashback" swell (noise through a sweeping filter, then a hard cut), and the 4 PM meeting opens with the same riser plus a half-second white flash, like memory.
- **Three lore cards (fictionalized):** *The Priority* (the hallway poster must become a framed photo of VP Walsh, ASAP, expedited "like an organ for transplant"), *The Handoff* (the IT intern, the remote, the broken conference table, the exit at a speed just under running), *The Bookshelf* (pristine spines, approximate quotes, "you cannot tell if he reads the books; you cannot tell when he is lying; these may be the same skill").
- Deck is now 32 cards by week 7.

## v0.6 · 2026-07-11 "The Whole Backlog"
All twelve researched backlog items, plus five new lore cards.
- **Death recap:** Burnout now itemizes exactly where your Sanity went, by source. Losses are legible.
- **Weekly contracts:** one optional objective each Monday (Clean Week, Archivist, Stoic, Diplomat, Ghost), +4 Leverage if met, shown under the stats bar and scored at week's end.
- **Card unlock schedule:** new saves start with 12 personnel files; the rest unlock weekly through week 7 ("PERSONNEL UPDATE: 3 NEW FILES ADDED TO YOUR ROTATION").
- **Weekly modifiers:** one policy drawn per week from week 2 (Audit Week: locks cost +1 but each locked item pays +1 Leverage; Quota Week: 15 Credit by Wednesday EOD or -8 Rep; Strategic Partner: the vendor starts active; or no update, "THIS IS NOT REASSURING").
- **The Expense Report:** an interactive Friday form that gains one field per week, forever, from DATE through BLOOD TYPE. Click each field; the autofill values are pre-approved.
- **Life Outside:** the Saturday store now shows the plant, the friend, and the hobby. Free evenings feed them; voluntolds starve them.
- **Narrator inversion:** below 30 Sanity, card text drifts, words repeat repeat, and the terminal begins describing your choices before you make them ("You have already decided. The terminal is simply waiting.").
- **Animations:** claimed items physically slide off your side of the table at the Meeting; paper trails slam a FILED ink stamp; the screen dims as Time runs out; Malicious Compliance releases exactly one balloon.
- **The Elevator (week 6+, once per save):** the doors are already open. Someone in a suit the color of the building nods at you, precisely once. The keyhole floor is not in the directory.
- **Five lore cards (fictionalized):** *The Farewell Cake* (turnover has tripled in three years, a statistic that lives in the frequency of cake), *Coffee with Nina* (a real ally; earn her trust once and The Network costs 10 less, she made a call), *Per Director Reed* (nobody under her likes her; Miller implements everything she thinks), *The Procurement Paradox* (eleven expedited plastic plants, month eleven of CRM review), *The Austerity Memo* (four-cent cups banned; two engraved parking signs installed the same afternoon).
- Store layout bug fixed: owned items no longer overflow the frame.
- Deck is now 29 cards + the elevator. Cast additions: Nina, Director Reed, Doug's colleagues in spirit.

## v0.5.1 · 2026-07-11 "Original Dread"
- Boot line rewritten: "YOUR WORK IS IMPORTANT. YOUR WORK IS MYSTERIOUS." was a near-quote of a certain TV show's corporate creed. Replaced with an original line that earns the same unease: **"THE BUILDING KNOWS WHAT YOU DO HERE. THAT MAKES ONE OF YOU."** Policy going forward: evoke the vibe, never quote it.
- Design doc gains a researched Idea Backlog (death recap, weekly contracts, card unlock schedule, the Expense Report, Life Outside panel, narrator inversion, item-steal animation, and more).

## v0.5 · 2026-07-10 "The Building Sounds Wrong"
- **Reactive music:** the ambient drone now tracks your state. As Sanity falls, the detuned twin beats faster (a slow shimmer becomes a nauseous wobble) and the minor second swells from a whisper to a presence. Week number raises the floor of the dread, so later weeks never sound fully calm. Losing players hear it before they see it.
- **CRT day-wipe:** each day ends by collapsing to a bright horizontal line, then the next morning powers on. A soft two-note chime marks the new day.
- **New SFX:** store purchases, vendor inbox pings, day chime, and a true typewriter on the boot screen: sharp metallic strike plus dull body thock per character, a faint carriage-return bell at each line end, slow deliberate pacing with occasional hesitations, as if the terminal is deciding what to tell you. Click anywhere to skip.
- **Miller leaves early (new mechanic):** 40% of evenings, his office is dark by 3:04 PM ("he left for a thing, he always has a thing") and paper trails cost 1 less that night. His absences are your openings.
- **New card, The Retrospective:** Miller trash-talks Director Reyes, retired three years, whose systems still run the department unattended. Nod along, or mention that mildly and end the meeting four minutes early.
- **New card, The Matcha Machine:** VP Walsh's kitchen-remodel non-negotiable, polished weekly, untouched for 14 months. Make the first matcha in over a year (+6 Sanity, "two wrongs, one perfect green tea") or add it to your quiet ledger of institutional waste (+1 Leverage, "the ledger is patient").
- Deck is now 24 cards, 10 dealt per run.

## v0.4.1 · 2026-07-10 "Doug"
- **New card, The Parallel Tracker:** Doug, the department budget manager, self-assigns a spreadsheet that tracks the spreadsheet that tracks the budget, announces it as an "initiative," and your name is in it, awaiting input, forever. Feed the tracker (+Rep, the trackers are breeding) or secretly automate his cells from your real spreadsheet (+6 Credit, Doug tells leadership his process improvements are paying off).
- Deck is now 22 cards, 10 dealt per run.

## v0.4 · 2026-07-10 "Standing"
- **The At Risk tracker is now always visible:** a chip in the stats bar shows the current target item all day, red-bordered while exposed, green "all secured" when everything is locked. The core mechanic can finally be planned around in the moment, as the design doc always intended.
- **Reputation matters at the edges (Decision 13):** below 30, evening voluntolds cannot be declined ("unavailable at your current standing"). At 70 or above, your best unlocked item is shielded at the Friday Meeting: his eyes slide off it like it's laminated. The shield never grants a free Malicious Compliance.
- **SFX:** tiny procedural sounds for choices, paper-trail locks, the REASSIGNED stamp, and burnout. Independent of the ambient toggle, still zero assets.
- **Help screen:** a "? HANDBOOK" button opens the Employee Handbook (Abridged). "This handbook is provided for your convenience. It is not a contract. Nothing is a contract."
- **Two new cards (fictionalized):** *The Head Start*, Director Brooks assigns you the task you started an hour ago ("Amazing turnaround!" The turnaround was negative one hour.). *The Escalation Path*, VP Walsh's chat status is green, he is typing, and the true business of his day is soccer; make the decision yourself and he hearts it eleven minutes after kickoff ends.
- Deck is now 21 cards, 10 dealt per run.

## v0.3.4 · 2026-07-10 "The Spiral and the Queue"
- **New card, The Circle-Back:** a vendor follows up, again, on an invoice that cannot renew for six months. They know. You told them. Twice. The cheap reply (+2 Credit) starts a recurring mechanic: for the rest of the week the vendor pings your inbox at the top of encounters (up to once a day, 2 Sanity each, rotating messages like "Bumping this to the top of your inbox!" It rises. It always rises.). The expensive option (the date in bold plus a calendar invite six months out) costs more now and buys actual silence.
- **New card, Ticket #4471:** filed in March, held "IN QUEUE" for four months, then closed with no work performed. Reopen it (it becomes ticket #6203, young and full of hope, ten business months out) or fix it yourself with an unauthorized ladder (+4 Credit, +2 Sanity, and Facilities closes the successor ticket anyway: "RESOLVED: no longer reproducible").
- Deck is now 19 cards, 10 dealt per run.

## v0.3.3 · 2026-07-10 "The Hum"
- **Ambient music, procedurally generated:** an eerie institutional drone built entirely in Web Audio (detuned low sines a minor second apart, slow breathing LFOs, a distant lonely tone every 9 seconds). No audio files, no licensing, nothing to credit. Toggle button fixed top-right ("AMBIENT: ON/OFF"); off by default, starts only on click per browser autoplay rules.
- **Save codes:** progress already auto-saves to the browser (localStorage). New EXPORT RECORD / IMPORT RECORD buttons on the boot screen turn the save into a copyable code, so players can back it up or move it between devices/browsers.
- **New card, A Visit to Human Resources:** Dana listens with enormous warmth; the file is not for you. Formal inquiries are forwarded, in full, with your name on them, to the person they are about. For transparency. "Off the record" is not a place.
- **Style pass:** every em dash removed from game text and README (replaced with commas, periods, colons, or middle dots). Deck is now 17 cards.

## v0.3.2 · 2026-07-10 "The Cancelled 1:1"
- The Wednesday 1:1 now has a 50% chance of being **cancelled four minutes before it starts**: no reason, no reschedule. "A performance conversation that never happens is a raise that never has to be declined." On cancellation: email your update anyway (+3 Credit, in writing, impossible to cancel) or take the free half hour (+4 Sanity).
- New choice in the 1:1 that survives: **ask about your performance review**: "Let's give it the time it deserves, offline." Ends the meeting nine minutes early; the only word that shortens meetings in this building.

## v0.3.1 · 2026-07-10 "Common Names & Publication Prep"
- **Cast renamed to common names:** Director Fenn → **Director Miller**; Micah → **Chris**; Aldous → **Dave** (from Records); Priya/Sam → **Sarah/Tom**. Terry from Facilities and The President unchanged.
- **Two new cards (fictionalized):** *The Clarification*, the boss appears unprompted to explain that nothing happened with someone in Marketing, "and what did happen was entirely from her side"; holding silence makes him re-explain, each version incriminating him slightly more. *The Line Item*, you administer the program budget, so you can see every salary in it, including the raise he's currently explaining you didn't get; quiet documentation pays +2 Leverage ("the spreadsheet has radicalized you").
- Deck is now 16 cards, 10 dealt per run.
- **GitHub-ready:** README rewritten for the HTML/JS game (play instructions, rules summary, fiction disclaimer); `deploy.yml` replaced with a no-build static Pages workflow (Settings → Pages → Source: "GitHub Actions").

## v0.3 · 2026-07-10 "The Weekend"
- **The Saturday store + Leverage meta-progression (Decision 11):** weeks now persist via localStorage. Every ending pays Leverage (Burnout pays 3, every run teaches the system's shape). Spent between weeks in a store framed as the outside world: 6 quality-of-life upgrades (home coffee +1 Time, out-of-pocket therapy -1 Sanity costs, boundaries book, templates folder, standing Micah lunch, +1 PTO) and a 3-item **Escape Plan** (Portfolio/Network/Savings, 80 Leverage total) that unlocks the **Two Weeks' Notice** true ending, handed to the boss mid-meeting, at 4:01 PM, where for the first time in institutional memory he looks up from his laptop.
- **Announced difficulty ramp (Decision 12):** in-fiction "policy updates" at boot. Wk2+: voluntold-heavy evenings. Wk3+: boss claims TWO items. Wk4+: Sanity costs +1. Wk5+: paper trails +1 Time. Cost-of-living line on the score screen also creeps 0.2%/week.
- **Two new lore cards (fictionalized):** *The Offboarding*, a termination meeting where the boss never once looks up from his laptop; walking the fired coworker out pays +3 Leverage, a currency the company has no field for. *The Rumor*, the boss invents an office romance for sport; kill it with specifics or watch the office get 4% worse forever.
- Deck is now 14 cards, 10 dealt per run. Save-wipe button on the boot screen.

## v0.2 · 2026-07-10 "The Full Week"
- **Engine pivot (Decision 9):** v1 is now vanilla HTML/JS. The game is `index.html` at repo root, GitHub Pages serves it with no build step. Godot skeleton shelved (safe to delete locally); the Godot-export GitHub Actions workflow is obsolete.
- **First fully playable run:** Monday–Friday, 10-card shuffled deck + fixed Standup (Mon) and 1:1 with the boss (Wed), evening events (free time / voluntold), At Risk tracker with paper-trail locks and Thursday-midnight lock-in, the Friday 4 PM Meeting boss encounter, and three endings: Burnout, Malicious Compliance, Week Survived (graded).
- **Severance-coded presentation:** dark terminal palette, CRT scanlines/flicker, typewriter boot sequence, day-title interstitials, animated stat deltas, REASSIGNED stamp animation at the Friday Meeting, burnout glitch effect. All CSS/JS, no assets, no dependencies.
- **New mechanics from playtest simulation (Decision 10):** week grade scores what the boss *took*, not what you earned, greedy max-credit play leaves no Time for paper trails and caps at grade C; protection-focused play reaches A. 1:1 "NOTICED" items cost +1 Time to lock. Crunch (Time = 0) raises Sanity costs 50%. Ally (Micah) grants one free lock per week. PTO skips a card for -3 Rep.

## v0.2.1 · 2026-07-10 "Lore Pass"
- Deck grown to 12 cards (10 dealt per run, two rest each week, adding run variety). Two new cards built from fictionalized systemic dynamics:
  - **A Dashboard for Upstairs**: your work travels to The President, an unseen executive nobody on the floor has met. Build it beautiful (+14 Credit, auto-NOTICED, it was built to travel) or watermark your name through it (+10, arrives pre-SECURED).
  - **The Realignment**: an entire second function is rerouted "through" your box with no comp. Absorb it (permanent -1 max Time/day, +Rep) or push back in writing (-Rep, -Sanity, keep your Time).
- **The Annual Appreciation Event**: fixed Thursday-evening beat: the year's entire appreciation, delivered in one evening at a driving-range complex with a fajita bar. The cruelest part is that it's fun.
- Friday Meeting verdict now names where the stolen Credit goes: Upstairs, to The President, who signs raises for whoever is standing nearest the work when it arrives.

## Unreleased (pre-v0.2)
- Repo scaffolded: Godot 4 project skeleton, folder structure, MIT license, asset-credit tracking, GitHub Actions HTML5-export workflow (draft).
- Design doc: vertical slice scope decided (5 Meeting Types + 5-6 task/fire cards for v1). Balance v0.1 numbers set (Sanity/Time/Credit/PTO/Reputation). See `Friday_Meeting_Design_Doc.md` Decisions Log items 6-7.
- No playable content yet, next milestone is a single playable day (Standup + Quick Sync + one task card) to sanity-check the card-encounter format before building out the full week.
